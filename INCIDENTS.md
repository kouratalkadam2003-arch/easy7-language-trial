# INCIDENTS.md — سجل الحوادث والمشاكل التاريخية وحلولها في Easy7

> **للمطورين والوكلاء المستقبليين:** يوثق هذا الملف المشاكل التقنية والتنسيقية الفعلية التي واجهها فريق التطوير والوكلاء بالتوازي وكيف حُلّت جذرياً لتجنب تكرارها.

---

## 1. دمج الدروس (2,520 ملفاً تعليمياً)

### المشكلة: اختلاف بنية ملفات JSON المحولة
- **التشخيص:** الدروس المستلمة من تيليجرام كانت مغلفة بداخل نصوص JSON داخلية.
- **الحل:** كتابة محول بايثون لفك الغلاف وبناء بنية مسطحة لـ 1,260 درساً عادياً و1,260 درس قصة سردية.

### المشكلة: ظهور الدروس القديمة من كاش المتصفح
- **التشخيص:** المتصفح كان يقرأ من IndexedDB بالمفتاح القديم `lesson_`.
- **الحل:** رفع مفتاح الكاش إلى `lesson_v2_` في `LessonPage.tsx` لإبطال أي كاش قديم تلقائياً.

---

## 2. أزمة الراديو AI والفويس شات على Netlify

### المشكلة: الراديو والفويس شات يعملان محلياً ويفشلان على Netlify
- **السبب الجذري:** النسخة السابقة كانت تتصل بـ WebSocket relay (`/ws-voice`) على خادم محلي. منصة Netlify استضافة ثابتة (Serverless) ولا تدعم خوادم WebSocket الخلفية.
- **الحل:** الاعتماد على الاتصال المباشر من المتصفح مع Gemini Live API عبر `@google/genai` (`ai.live.connect`) ومعالجة الصوت عبر Web Audio API (PCM 24kHz).

### المشكلة: خطأ 404 Not Found عند توليد محتوى الراديو
- **السبب الجذري:** استدعاء REST `generateContent` بموديل `gemini-3.1-flash-live-preview` (وهو موديل مخصص حصرياً للبث ثنائي الاتجاه Live WebSocket).
- **الحل:** استبداله بـ `TEXT_MODEL` (`gemini-3.6-flash`) لتوليد النصوص، وترقية صوت الـ TTS إلى `gemini-3.1-flash-tts-preview`.

### المشكلة: خالد يتحدث وسارة صامتة
- **السبب الجذري:** جلسة بث واحدة فقط بصوت واحد.
- **الحل:** بناء نظام تتابع ذكي (Dual-Host Alternating Relay) يفتح جلستين منفصلتين لسارة وخالد بالتناوب السلس.

---

## 3. السبتيتيل واللغات والتحكم بالمستوى

### المشكلة: خلط نصوص سارة وخالد في السبتيتيل
- **الحل:** عزل نصوص كل متحدث في المستطيل الخاص به (سارة في الأعلى وخالد في الأسفل) مع إضاءة حواف المستطيل أثناء الحديث.

### المشكلة: ظهور الحروف اليابانية/الصينية المعقدة افتراضياً
- **الحل:** جعل الروماجي والبينيين افتراضياً مع توفير زر تبديل `Languages 🔤` للكانجي والهانزي، وإضافة نطق صوتي بالأحرف العربية (`toArabicPhonetics`).

### المشكلة: عدم وجود زر خروج أو تبديل اللغة والمستوى
- **الحل:** إضافة زر تسجيل الخروج `logout()`، ونافذة `LanguageSelectModal` لاختيار اللغات الـ 7، ومحرك `cefrGuidelines.ts` لمعايرة كلام الذكاء الاصطناعي من A0 إلى C2.
